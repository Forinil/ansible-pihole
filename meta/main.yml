---

dependencies:
  # depend on Apache, but only for the host that runs Heimdall
  - role: apache
    become: true
    tags: apache
    when: 
      - ansible_lsb.id == "Raspbian"
      - ansible_hostname == "alexandria"
